# Reports

Published reports should live under `docs/reports/` for GitHub Pages delivery.

Recommended pattern:
- one HTML file per report
- self-contained styling when possible
- source inventory and drafts should live outside this repo
